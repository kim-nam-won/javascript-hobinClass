const $fruitSelect = document.getElementById("fruitSelect");

// 값이 변경될때마다 조건을 실행하는 조건은 change 이벤트를 사용한다.
// change 이벤트는 값이 변경된 후에 발생한다.
// $fruitSelect.addEventListener("change", (event) => {
//   console.log(event.target.value);
// });

// remove 매서드 dom 객체의 값을 삭제함.
// $fruitSelect.remove(0); // 0번째 인덱스의 값을 삭제함.
